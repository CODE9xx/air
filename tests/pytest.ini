[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: тесты, требующие полного стека (PostgreSQL + Redis + Worker)
    security: security-тесты
    e2e: end-to-end тесты
addopts = -v --tb=short
