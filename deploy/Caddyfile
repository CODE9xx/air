{
    email kyzx@yandex.ru
}

# Keep Code9 and Codenine routing isolated. Each imported file owns only its
# own domains so future changes can be reviewed and committed independently.
import /etc/caddy/sites-enabled/code9.caddy
import /etc/caddy/sites-enabled/codenine.caddy
