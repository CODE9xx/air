# packages/shared

Общие TS-типы и JSON-схемы, которые используются и в `apps/web`, и в `apps/api` (через codegen).

**Owner:** Backend + Frontend (общий пакет, меняется через CHANGE_REQUESTS).
**Wave 1:** пустая заготовка.
**Wave 2:** типы для API-responses, enum'ы статусов, zod-схемы валидации.
