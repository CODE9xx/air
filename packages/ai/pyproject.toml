[project]
name = "code9-ai"
version = "0.1.0"
description = "Code9 AI helper-функции (mock insights, anonymizer stub)."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
include = ["packages_ai*"]
